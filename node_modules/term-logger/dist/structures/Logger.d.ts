/**
 * Logger module for logging messages with different types and colors.
 */
/**
 * Logger object that contains logging functions for each log type.
 */
export declare const Logger: Record<"cluster" | "command" | "complete" | "critical" | "database" | "debug" | "error" | "event" | "fatal" | "info" | "note" | "pending" | "ready" | "shard" | "start" | "success" | "waiting" | "warn" | "watch", (message: string) => void>;
